# 账号清单：本仓库（driver）要同步到的所有 GitHub 账号
#
# 格式：  <账号名>[:<SSH 别名>]
#
# 规则：
#   1. 第一个账号为「主账号」(primary)，只有它会 write/commit 归档结果；
#      其余为镜像仓，单向同步，避免多处写导致 git 历史冲突。
#   2. SSH 别名对应 ~/.ssh/config 里的 Host，用于本机推送时区分不同账号的密钥。
#      省略时回退到 github.com（即 ~/.ssh/config 的默认 Host）。
#      本机实测可用的别名（都走 443 端口，规避 22 端口被封）：
#        github.com              -> Hi edidada!
#        edidadaoutlook.com      -> Hi edidadaoutlook!
#        wiseism126_github.com   -> Hi wiseism!
#        wdidada126_github.com   -> Hi wdidada126!
#   3. 行内 # 之后为注释，空行忽略。
#   4. 云端同步（mirror.yml）走 HTTPS + MIRROR_TOKEN，与本文件的 SSH 别名无关。

edidada:github.com              # 主账号：私有源 cpp_ecshop 的归属账号
edidadaoutlook:edidadaoutlook.com
wiseism:wiseism126_github.com
wdidada126:wdidada126_github.com
