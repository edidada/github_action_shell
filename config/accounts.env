# 账号清单：本仓库（driver）要同步到的所有 GitHub 账号
# 规则：
#   1. 第一个账号为「主账号」(primary)，只有它会 write/commit 归档结果；
#      其余为镜像仓，只读同步，避免多处写导致 git 历史冲突。
#   2. 行内 # 之后为注释，空行忽略。
#   3. 主账号下的仓库才需要配置 MIRROR_TOKEN。

edidada            # 主账号：私有源 cpp_ecshop 的归属账号
edidadaoutlook     # 镜像
wiseism            # 镜像
wdidada126         # 镜像
